#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "archiver.h"
#include <check.h>

void main(int argc, char *argv[]){
    
    char * fileIn = (char *) malloc (1000 * sizeof(char));
    char *outputFileName = (char *) malloc (1000 * sizeof (char));
    sprintf (outputFileName, "%sMYTAR", file);
    
    void write_archive_TRANSPLANT_RESULT;
    write_archive_TRANSPLANT_RESULT = write_archive_GRAFT_INTERFACE ( argv, argc, outputFileName, fileIn );
}

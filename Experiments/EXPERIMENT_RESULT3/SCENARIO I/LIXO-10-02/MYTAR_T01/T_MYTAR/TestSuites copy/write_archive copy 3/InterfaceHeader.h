
#include <stdio.h>

#include <stdlib.h>

#include <unistd.h>

#include <stdint.h>

#include <getopt.h>

#include <sys/types.h>

#include <sys/stat.h>

#include <fcntl.h>

#include <errno.h>

#include <string.h>

#include <sys/mman.h>

#include <util.h>
typedef    short    bool;
typedef    short    line;
#define    getline    xgetline
void write_archive_GRAFT_INTERFACE (getline (*addr1), char *$_host_p, char *$_host_fileOutput, char *$_host_fileIn, bool $_host_hadpr, int $_host__exitoneof, int $_host_noprompt, int $_host_cnt, int $_host_seensemi, int $_host_given, int $_host_lchng, int $_host_c, line *$_host_addr);
